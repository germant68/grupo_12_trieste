<%- include('../partials/head') %>
<body>
    <%- include('../partials/header') %>
    <main>
        <section>
            <div>
                <h2>REGISTRATE</h2><img src="./images/music_icon.png" id="disco" alt="music_icon">
            </div>
            <div>
                <br><h6>(*) Campos Requeridos</h6>
                <% if (locals.errores) { %>
                    <% errores.forEach(e => { %>
                        <p class="error_msg_form"><i class="fa-solid fa-triangle-exclamation"></i> - <%= e.msg %></p>
                    <% } )%>
                <% }%>
                <% if (locals.msje) { %>
                    <p class="success_msg_form"><i class="fa-solid fa-triangle-exclamation"></i> - <%= locals.msje %></p>
                <% }%>
                <br>

                <form action="/registro" method="POST" class="form_reg">
                    <!-- <label for="usuario_reg">Usuario*</label>
                    <input type="text" name="usuario_reg" value="<%= locals.prev && prev.usuario_reg ? prev.usuario_reg: '' %>" required>
                    <br>-->
                    <label for="">Dirección de correo electrónico*</label>
                    <input type="email" name="email_reg" value="<%= locals.prev && prev.email_reg ? prev.email_reg: '' %>" required>
                    <br>
                    <label for="">Nombre*</label>
                    <input type="text" name="nombre_reg" value="<%= locals.prev && prev.nombre_reg ? prev.nombre_reg : '' %>" required>
                    <br>
                    <label for="">Apellido*</label>
                    <input type="text" name="apellido_reg" value="<%= locals.prev && prev.apellido_reg ? prev.apellido_reg: '' %>" required>
                    <br>
                    <label for="pwd_reg">Contraseña*</label>
                    <input type="password" name="pwd_reg" id="pwd_reg" required> 
                    <br>
                    <label for="pwdrepeat">Repita su Contraseña*</label>
                    <input type="password" name="pwdrepeat" id="pwdrepeat" required> 
                    <br>
                    <div>
                    <input type="checkbox" name="newsletter" value="<%= locals.prev && prev.newsletter ? prev.newsletter: '' %>"> <label> Suscribite a nuestra newsletter para recibir novedades</label>
                    </div>
                    <br>
                    <div>
                        <input type="submit" value="Enviar" name="submit_reg">
                        <input type="reset" value="Borrar" name="reset_reg">
                    </div>
                </form>
            </div>

        <br>
        </section>
        
    </main>

    <%- include('../partials/footer') %>
</body>
</html>